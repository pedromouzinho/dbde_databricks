# =============================================================================
# requirements.txt — DBDE AI Assistant (Databricks Edition)
# =============================================================================
# Removed: azure-identity (handled by Databricks), azure-specific SDKs
# Added: openai (for Foundation Model APIs), asyncpg (for Lakebase)
# =============================================================================

# --- Core framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.22

# --- LLM client (OpenAI-compatible API) ---
openai>=1.40.0

# --- HTTP client (for external APIs: DevOps, Figma, Miro) ---
httpx==0.27.2

# --- Database (Lakebase / Postgres) ---
asyncpg>=0.29.0

# --- Data processing ---
pandas==2.2.3
numpy==2.1.3
duckdb==1.5.0

# --- Tabular / Excel reading (fast Rust reader; pandas+openpyxl is the fallback) ---
python-calamine==0.2.3
xlrd==2.0.1

# --- File generation ---
openpyxl==3.1.5
fpdf2==2.8.1
python-pptx==0.6.23
python-docx==1.1.2
xlsxwriter==3.2.0

# --- Visualization ---
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.24.1

# --- Token counting ---
tiktoken>=0.9

# --- PDF processing ---
pypdf==6.9.1
pdfplumber==0.11.4

# --- SEPA C2B validator (XSD validation, pure-python) ---
xmlschema>=3.0

# --- Rate limiting ---
slowapi==0.1.9
